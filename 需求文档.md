# Excel 视频时长统计工具 - 最终需求文档

版本：v1.0　日期：2026-08-01

## 1. 项目概述

一个桌面小工具：用户把一个 Excel 文件拖到程序窗口上，程序自动读取文件，
把表中每一行的"视频时长"累加，弹窗显示总时长（总小时 / 总分钟 / 总秒），
并把合计写回表格。纯本地离线运行，不联网，不上传任何数据。

## 2. 用户操作流程

1. 把 `.xlsx` / `.xls` / `.csv` 文件拖到程序窗口；
   （同时提供"选择文件"按钮作为备选方式）
2. 若文件内有多个工作表（Sheet），弹出列表让用户选择；只有一个则自动选中；
3. 程序读取 A 列（A1 起）每一行的时长；
4. 计算总和，弹窗显示结果；
5. 在表格数据末尾追加一行"总计"，并保存文件；
6. 程序保持打开，可继续拖入下一个文件，无需重启。

## 3. 时间格式支持（核心）

视频时长**没有"小时"**，最长不超过 10 分钟。支持两种来源：

### 3.1 文本格式
- `3:40` → 3 分 40 秒
- `3:00` → 3 分 0 秒（用户写 3 分钟的视频，就写 `3:00`）
- `3:40:25`（三段）→ 兜底按 `时:分:秒` 解析（无小时场景一般不出现）
- 纯数字 `90` → 按 **90 秒** 解析（= 1 分 30 秒）

### 3.2 Excel 时间单元格格式
- Excel 把 `3:40` 自动存成时间值时，按 **MM:SS** 解释，
  即显示值 `3:40` → 3 分 40 秒；
- 显示值 `23:59` → 23 分 59 秒（不超过小时）。

### 3.3 解析规则
- 内部统一换算成"秒"再累加，避免进位误差；
- 解析不了的单元格（表头文字、姓名、备注、空行）自动跳过，
  并在结果里提示"跳过 N 行"。

## 4. 统计范围

- 仅统计 **A 列**，从 A1 开始到 A 列最后一个有内容的单元格；
- 每一行 = 一条视频的时长，逐行累加。

## 5. 计算与显示

### 5.1 计算
- 总秒数 = 所有行解析秒数之和；
- 换算：总小时（小数形式，如 30 分钟 = `0.5h`）、总分钟、总秒数。

### 5.2 弹窗显示（换行列出）
```
共统计：12 行（跳过 1 行）
总小时：0.5h
总分钟：30 分钟
总秒数：1800 秒
```

## 6. 写回 Excel

- 在 A 列最后一个数据行的下一行写入合计：
  `总计：0.5h（0 小时 30 分 0 秒）`
- 保存为原文件；不覆盖原始数据（总计行单独追加，可手动删除）。

## 7. 非功能需求

- 纯本地、离线、单机运行，不联网；
- Windows 下双击 exe 即可运行，无需用户安装任何环境；
- 使用虚拟环境安装依赖，用 PyInstaller 打包成**单个 exe**；
- 无控制台黑窗口；错误信息用中文弹窗友好提示；
- 界面简洁，对非技术人员友好。

## 8. 技术方案

- 语言：Python 3
- 界面：tkinter（Python 自带）
- 拖拽：tkinterdnd2
- 读表：openpyxl（`.xlsx`）、xlrd（`.xls`）、csv 标准库（`.csv`）
- 打包：PyInstaller `--onefile --noconsole`
- 项目结构：
  ```
  excel-time-calculator/
    main.py            # 入口 + 界面
    reader.py          # 读取文件、解析时间
    calc.py            # 求和与格式化
    requirements.txt
    build.bat          # 一键打包脚本
  ```

## 9. 已确认约定

- `3:40` = 3 分 40 秒（MM:SS），无小时；
- 视频最长不超过 10 分钟；
- 用户写 3 分钟的视频时输入 `3:00`；
- 纯数字按秒解析；
- 只统计 A 列，逐行累加；
- 结果既弹窗显示，也写回 Excel 表格末尾；
- 弹窗不显示 Excel 原始数值。

## 10. 版本记录

- v1.0：最终需求定稿（2026-08-01）
